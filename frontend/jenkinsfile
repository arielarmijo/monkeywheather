pipeline {
	agent any
	stages {
		stage('build') {
			steps {
				sh 'ls -l'
			}
		}
		stage('deploy') {
			steps {
				sh 'rm -r /var/lib/tomcat9/webapps/monkeyweather'
				sh 'cp -r frontend /var/lib/tomcat9/webapps/monkeyweather/'
			}
		}
	}
}
